// Allow `import Combinable` to replace `import Combine`
@_exported import Combine

/// Combinable adds several new operators to all Combine publishers with extensions to the
/// `Publisher` protocol.
///
public extension Publisher {
}
